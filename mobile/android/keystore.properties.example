# Copy this file to keystore.properties (same directory) and fill in the
# real values. keystore.properties itself is gitignored -- never commit it.
# See mobile/README.md for how to generate the keystore this file points at.
storeFile=anatolia-q-release.jks
storePassword=CHANGE_ME
keyAlias=anatolia-q
keyPassword=CHANGE_ME
