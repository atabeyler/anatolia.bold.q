import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.boldkimya.anatoliaq',
  appName: 'ANATOLIA-Q',
  webDir: '../client/dist'
};

export default config;
